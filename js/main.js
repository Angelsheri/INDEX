/* main init — runs after all components are loaded */
document.addEventListener('DOMContentLoaded', function () {
  // Guard body class so header logic can style header state
  document.body.classList.add('js-ready');
});
